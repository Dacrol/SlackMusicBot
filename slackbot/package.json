{
  "name": "slackbot",
  "version": "0.0.0",
  "main": "bot.js",
  "author": "Dacrol",
  "private": true,
  "dependencies": {
    "@slack/rtm-api": "^5.0.2",
    "dotenv": "^8.1.0"
  },
  "scripts": {
    "start": "node bot.js",
    "dev": "nodemon --config nodemon.json --exec \"npm run debug\"",
    "debug": "node --inspect=9229 bot.js"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
